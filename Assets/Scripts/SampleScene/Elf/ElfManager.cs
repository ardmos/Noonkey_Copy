using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 엘프 매니저. 
/// 
/// 하는 일 : 현재 활성화되어있는 전체 엘프들의 초당 회복량을 통합하여 처리한다(각각 엘프들의 초당 회복량 변동시마다 보고 필요.) (실체 회복량 적용(playerData.heartOneSec 에 적용시키면 된다.))
/// 
/// </summary>

public class ElfManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
